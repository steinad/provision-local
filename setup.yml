- name: a playbook to set up my local machine
  hosts: localhost
  connection: local
  roles:
    - common
  vars:
    download_dir: "{{ ansible_env.HOME }}/Downloads"
    local_bin_dir: "{{ ansible_env.HOME }}/.local/bin"
    apps_dir: "{{ ansible_env.HOME }}/Applications"
    desktop: true
    versions:
      zsh: '5.7.1'
      tmux: '3.0'
      vivaldi: 'snapshot_2.7.1609.4-1'
      ripgrep: '11.0.1'

  # tasks:
  #   - debug:
  #       msg: "{{ ansible_env }}"
  #   - debug:
  #       msg: "{{ ansible_facts }}"
